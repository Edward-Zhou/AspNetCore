@model MVCPro.Models.TranslationsModel

@{
    ViewData["Title"] = "Translation";
}

<h2>Translation</h2>

<div>
    <h4>TranslationsModel</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<div>
    <table>
        @for (int i = 0; i < Model.Translations.Count; i++)
        {
            <tr>
                <td>
                    <input asp-for="@Model.Translations[i].LanguageID" type="hidden" />
                    <input asp-for="@Model.Translations[i].LanguageID" />
                    <input asp-for="Translations[i].TranslationID" />
                    <input asp-for="Translations[i].LanguageID" />
                </td>
                <td>
                    <input asp-for="@Model.Translations[i].Value" class="form-control" />
                </td>
            </tr>
        }
    </table>
    <a asp-action="Index">Back to List</a>
</div>
